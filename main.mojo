#test for throughpout
fn estimate_pi(samples: Int) -> Float64:
	# stub
	 return -1.0

fn main():
    var samples: Int = 10000
    var result: Float64 = estimate_pi(samples)
    print("Estimated Pi (", samples, " samples): ", result)

